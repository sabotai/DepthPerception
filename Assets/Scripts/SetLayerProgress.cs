using UnityEngine;
using System.Collections;

public class SetLayerProgress : MonoBehaviour {

	//this script should take the playerprefs for the current layer and set the progressive setting
	//for the mixing of the two spaces



	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
